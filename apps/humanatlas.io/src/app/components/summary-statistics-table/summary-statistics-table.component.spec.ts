import { SummaryStatisticsTableComponent } from './summary-statistics-table.component';

describe('SummaryStatisticsTableComponent', () => {
  it('passes a dummy test', () => {
    expect(SummaryStatisticsTableComponent).toBeDefined();
  });
});
