import { OntologyTreeComponent } from './ontology-tree.component';

describe('OntologyTreeComponent', () => {
  it('passes a dummy test', () => {
    expect(OntologyTreeComponent).toBeDefined();
  });
});
