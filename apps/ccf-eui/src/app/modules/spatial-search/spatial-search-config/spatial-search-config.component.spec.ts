import { SpatialSearchConfigComponent } from './spatial-search-config.component';

describe('SpatialSearchConfigComponent', () => {
  it('passes a dummy test', () => {
    expect(SpatialSearchConfigComponent).toBeDefined();
  });
});
