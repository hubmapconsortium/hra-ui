import { SpatialSearchInputsComponent } from './spatial-search-inputs.component';

describe('SpatialSearchInputsComponent', () => {
  it('passes a dummy test', () => {
    expect(SpatialSearchInputsComponent).toBeDefined();
  });
});
