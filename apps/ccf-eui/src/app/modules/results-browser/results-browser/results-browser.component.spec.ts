import { ResultsBrowserComponent } from './results-browser.component';

describe('ResultsBrowserComponent', () => {
  it('passes a dummy test', () => {
    expect(ResultsBrowserComponent).toBeDefined();
  });
});
