import { FiltersContentComponent } from './filters-content.component';

describe('FiltersContentComponent', () => {
  it('passes a dummy test', () => {
    expect(FiltersContentComponent).toBeDefined();
  });
});
