import { TrackingPopupComponent } from './tracking-popup.component';

describe('TrackingPopupComponent', () => {
  it('passes a dummy test', () => {
    expect(TrackingPopupComponent).toBeDefined();
  });
});
