import { SidenavHeaderComponent } from './sidenav-header.component';

describe('SidenavHeaderComponent', () => {
  it('passes a dummy test', () => {
    expect(SidenavHeaderComponent).toBeDefined();
  });
});
