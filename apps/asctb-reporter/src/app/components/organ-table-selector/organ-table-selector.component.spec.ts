import { OrganTableSelectorComponent } from './organ-table-selector.component';

describe('OrganTableSelectorComponent', () => {
  it('passes a dummy test', () => {
    expect(OrganTableSelectorComponent).toBeDefined();
  });
});
