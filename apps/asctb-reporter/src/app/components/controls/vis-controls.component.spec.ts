import { VisControlsComponent } from './vis-controls.component';

describe('VisControlsComponent', () => {
  it('passes a dummy test', () => {
    expect(VisControlsComponent).toBeDefined();
  });
});
