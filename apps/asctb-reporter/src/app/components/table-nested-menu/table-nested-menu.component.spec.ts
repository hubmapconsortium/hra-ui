import { TableNestedMenuComponent } from './table-nested-menu.component';

describe('TableNestedMenuComponent', () => {
  it('passes a dummy test', () => {
    expect(TableNestedMenuComponent).toBeDefined();
  });
});
