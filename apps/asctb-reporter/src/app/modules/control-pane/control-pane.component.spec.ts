import { ControlPaneComponent } from './control-pane.component';

describe('ControlPaneComponent', () => {
  it('passes a dummy test', () => {
    expect(ControlPaneComponent).toBeDefined();
  });
});
