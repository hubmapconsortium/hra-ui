import { FilterMenuOverlayComponent } from './filter-menu-overlay.component';

describe('FilterMenuOverlayComponent', () => {
  it('passes a dummy test', () => {
    expect(FilterMenuOverlayComponent).toBeDefined();
  });
});
