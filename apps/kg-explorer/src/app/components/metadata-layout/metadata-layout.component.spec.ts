import { MetadataLayoutHeaderComponent } from './metadata-layout.component';

describe('MetadataLayoutHeaderComponent', () => {
  it('passes a dummy test', () => {
    expect(MetadataLayoutHeaderComponent).toBeDefined();
  });
});
