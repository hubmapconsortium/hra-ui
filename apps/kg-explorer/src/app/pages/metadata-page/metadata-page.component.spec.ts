import { MetadataPageComponent } from './metadata-page.component';

describe('MetadataPageComponent', () => {
  it('passes a dummy test', () => {
    expect(MetadataPageComponent).toBeDefined();
  });
});
