import { MetadataDonorFormComponent } from './metadata-donor-form.component';

describe('MetadataDonorFormComponent', () => {
  it('passes a dummy test', () => {
    expect(MetadataDonorFormComponent).toBeDefined();
  });
});
