import { MetadataAuthorFormComponent } from './metadata-author-form.component';

describe('MetadataAuthorFormComponent', () => {
  it('passes a dummy test', () => {
    expect(MetadataAuthorFormComponent).toBeDefined();
  });
});
