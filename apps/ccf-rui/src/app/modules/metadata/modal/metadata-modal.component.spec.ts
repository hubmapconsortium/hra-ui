import { MetadataModalComponent } from './metadata-modal.component';

describe('MetadataModalComponent', () => {
  it('passes a dummy test', () => {
    expect(MetadataModalComponent).toBeDefined();
  });
});
