import { CellPopulationGraphComponent } from './cell-population-graph.component';

describe('CellPopulationGraphComponent', () => {
  it('passes a dummy test', () => {
    expect(CellPopulationGraphComponent).toBeDefined();
  });
});
