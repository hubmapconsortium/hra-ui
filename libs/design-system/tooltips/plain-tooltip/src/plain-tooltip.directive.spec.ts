import { PlainTooltipDirective } from './plain-tooltip.directive';

describe('PlainTooltipDirective', () => {
  it('passes a dummy test', () => {
    expect(PlainTooltipDirective).toBeDefined();
  });
});
