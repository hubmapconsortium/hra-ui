import { ChipsGlobalStylesComponent } from './global-styles.component';

describe('ChipsGlobalStylesComponent', () => {
  it('passes a dummy test', () => {
    expect(ChipsGlobalStylesComponent).toBeDefined();
  });
});
