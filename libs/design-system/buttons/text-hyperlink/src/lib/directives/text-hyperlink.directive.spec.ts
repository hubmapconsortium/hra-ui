import { TextHyperlinkDirective } from './text-hyperlink.directive';

describe('TextHyperlinkDirective', () => {
  it('passes a dummy test', () => {
    expect(TextHyperlinkDirective).toBeDefined();
  });
});
