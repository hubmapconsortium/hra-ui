import { CheckboxErrorVariantDirective } from './checkbox-error-variant-directive';

describe('CheckboxErrorVariantDirective', () => {
  it('passes a dummy test', () => {
    expect(CheckboxErrorVariantDirective).toBeDefined();
  });
});
