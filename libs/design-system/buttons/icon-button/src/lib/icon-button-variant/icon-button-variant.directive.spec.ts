import { IconButtonVariantDirective } from './icon-button-variant.directive';

describe('IconButtonVariantDirective', () => {
  it('passes a dummy test', () => {
    expect(IconButtonVariantDirective).toBeDefined();
  });
});
