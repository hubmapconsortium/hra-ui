import { WorkflowCardActionsComponent } from './workflow-card.component';

describe('WorkflowCardActionsComponent', () => {
  it('passes a dummy test', () => {
    expect(WorkflowCardActionsComponent).toBeDefined();
  });
});
