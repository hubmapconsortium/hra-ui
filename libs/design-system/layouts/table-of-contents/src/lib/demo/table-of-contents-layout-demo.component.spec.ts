import { TableOfContentsLayoutDemoComponent } from './table-of-contents-layout-demo.component';

describe('TableOfContentsLayoutDemoComponent', () => {
  it('passes a dummy test', () => {
    expect(TableOfContentsLayoutDemoComponent).toBeDefined();
  });
});
