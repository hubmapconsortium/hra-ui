import { StepIndicatorComponent } from './step-indicator.component';

describe('StepIndicatorComponent', () => {
  it('passes a dummy test', () => {
    expect(StepIndicatorComponent).toBeDefined();
  });
});
