import { ScrollbarStylesComponent } from './scrollbar-styles.component';

describe('ScrollbarStylesComponent', () => {
  it('passes a dummy test', () => {
    expect(ScrollbarStylesComponent).toBeDefined();
  });
});
