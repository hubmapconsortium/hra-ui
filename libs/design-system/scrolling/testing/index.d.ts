/**
 * Setup the testing environment for scrolling to work.
 * Adds global mocks for functions used by scrolling if they don't exist.
 */
declare function setupScrollTesting(): void;

export { setupScrollTesting };
