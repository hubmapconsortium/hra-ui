import { CopyableUrlContainerComponent } from './copyable-url-container.component';

describe('CopyableUrlContainerComponent', () => {
  it('passes a dummy test', () => {
    expect(CopyableUrlContainerComponent).toBeDefined();
  });
});
