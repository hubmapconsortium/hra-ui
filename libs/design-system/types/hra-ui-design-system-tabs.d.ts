import { EnvironmentProviders } from '@angular/core';

/**
 * Applies global configuration for material UI tabs.
 * @returns Providers with global configuration.
 */
declare function provideTabs(): EnvironmentProviders;

export { provideTabs };
