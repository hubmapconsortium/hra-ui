import { DashboardComponentOutletDirective } from './dashboard-outlet.directive';

describe('DashboardComponentOutletDirective', () => {
  it('passes a dummy test', () => {
    expect(DashboardComponentOutletDirective).toBeDefined();
  });
});
