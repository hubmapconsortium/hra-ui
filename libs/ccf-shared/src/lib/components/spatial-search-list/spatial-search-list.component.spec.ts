import { SpatialSearchListComponent } from './spatial-search-list.component';

describe('SpatialSearchListComponent', () => {
  it('passes a dummy test', () => {
    expect(SpatialSearchListComponent).toBeDefined();
  });
});
